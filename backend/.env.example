# ===========================================
# SQL-AGENT Backend - Environment Variables
# ===========================================

# === SUPABASE (REST API) ===
# URL del proyecto Supabase (sin / al final)
SUPABASE_URL=https://your-project.supabase.co

# API Key publica (para llamadas autenticadas)
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Service Role Key (bypass RLS - MANTENER SECRETO)
SUPABASE_SERVICE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Conexion directa PostgreSQL (opcional, para checkpointer)
# POSTGRES_URL=postgresql://postgres:password@db.xxx.supabase.co:5432/postgres

# === LLM - GEMINI (primario) ===
GEMINI_API_KEY=AIzaSy...
GEMINI_MODEL=gemini-2.0-flash-exp

# === LLM - OPENROUTER (fallback/alternativo) ===
# Si Gemini tiene rate limit, usa OpenRouter como fallback
OPENROUTER_API_KEY=sk-or-v1-...
OPENROUTER_MODEL=google/gemini-3-flash-preview
# Usar OpenRouter como primario en lugar de Gemini
USE_OPENROUTER_PRIMARY=false

# === LANGSMITH (observabilidad) ===
LANGCHAIN_TRACING_V2=true
LANGCHAIN_PROJECT=sql-agent
LANGSMITH_API_KEY=lsv2_pt_...

# === SERVER ===
PORT=8000
FRONTEND_URL=http://localhost:3000

# === MODO DEMO ===
# Usar datos de ejemplo sin DB ni LLM
DEMO_MODE=false

# === TIMEOUTS ===
LLM_TIMEOUT_SECONDS=60
DB_TIMEOUT_SECONDS=30

# === LOGGING ===
# Niveles: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
# Formatos: json (produccion), text (desarrollo)
LOG_FORMAT=text

# === CACHE ===
# Deshabilitar cache para debugging (cada request es unico)
CACHE_ENABLED=true

# === n8n RAG (opcional) ===
# N8N_BASE_URL=https://horsepower-n8n.e5l6dk.easypanel.host
# N8N_RAG_WEBHOOK_PATH=/webhook/rag-products
